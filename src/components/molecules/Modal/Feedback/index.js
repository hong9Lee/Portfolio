import React from 'react';
import { useDispatch, useSelector } from 'react-redux';
const Feedback = () => {
  // const currentSetting = useSelector(state => state.settingMenu);
  const dispatch = useDispatch();
  return (
    <>
    </>
  );
};

export default Feedback;
